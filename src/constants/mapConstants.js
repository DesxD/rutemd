/**
 * Константы для работы с картой и маршрутами
 * Содержит координаты центров городов, цвета для маршрутов и прочие константы
 */

// Координаты центров городов
export const CITY_CENTERS = {
  edinet: { center: [48.1724, 27.3030], zoom: 14 },
  balti: { center: [47.7632, 27.9285], zoom: 14 },
  chisinau: { center: [47.0105, 28.8638], zoom: 13 },
  soroca: { center: [48.1569, 28.2853], zoom: 14 }
};

// Цвета для маршрутов
export const ROUTE_COLORS = [
  '#FF5733', '#33FF57', '#3357FF', '#F033FF', '#FF33A8', '#33FFF6',
  '#FF8C33', '#33A2FF', '#9933FF', '#FF3367', '#33FFD1', '#FF33D4'
];

// Настройки для выбранного маршрута
export const SELECTED_ROUTE_STYLE = {
  weight: 7,
  opacity: 0.9
};

// Настройки для невыбранного маршрута
export const DEFAULT_ROUTE_STYLE = {
  weight: 4,
  opacity: 0.6
};

// Константы для геолокации и навигации
export const LOCATION_SETTINGS = {
  // Расстояние, при котором точка маршрута считается достигнутой (в метрах)
  POINT_REACHED_THRESHOLD: 30,
  
  // Расстояние, при котором считается, что пользователь находится на маршруте (в метрах)
  ON_ROUTE_THRESHOLD: 100,
  
  // Частота обновления геолокации (в миллисекундах)
  UPDATE_INTERVAL: 1000,
  
  // Уровень масштабирования для режима следования
  FOLLOW_ZOOM: 16
};

// Константы для аудио-навигации
export const AUDIO_SETTINGS = {
  // Минимальный интервал между голосовыми сообщениями (в миллисекундах)
  MIN_ANNOUNCEMENT_INTERVAL: 10000,
  
  // Расстояние, при котором точка объявляется (в метрах)
  ANNOUNCEMENT_DISTANCE: 50,
  
  // Язык для голосовых сообщений
  LANGUAGE: 'ru-RU'
};